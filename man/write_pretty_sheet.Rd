% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write-format.R
\name{write_pretty_sheet}
\alias{write_pretty_sheet}
\title{Write a data frame and apply a \code{gs_theme()} in one call}
\usage{
write_pretty_sheet(
  ss,
  data,
  sheet = NULL,
  range = NULL,
  col_names = TRUE,
  theme = NULL
)
}
\arguments{
\item{ss}{Something that identifies a Google Sheet:
\itemize{
\item its file id as a string or \code{\link[googledrive:drive_id]{drive_id}}
\item a URL from which we can recover the id
\item a one-row \code{\link[googledrive:dribble]{dribble}}, which is how googledrive
represents Drive files
\item an instance of \code{googlesheets4_spreadsheet}, which is what \code{\link[googlesheets4:gs4_get]{gs4_get()}}
returns
}

Processed through \code{\link[googlesheets4:as_sheets_id]{as_sheets_id()}}.}

\item{data}{A data frame.}

\item{sheet}{Sheet to write into, in the sense of "worksheet" or "tab". You can identify a sheet by name, with a string, or by position, with a number. Ignored if the sheet is specified via \code{range}. If neither argument specifies the sheet, defaults to the first visible sheet.}

\item{range}{Where to write. This \code{range} argument has important similarities
and differences to \code{range} elsewhere (e.g. \code{\link[googlesheets4:range_read]{range_read()}}):
\itemize{
\item Similarities: Can be a cell range, using A1 notation ("A1:D3") or using
the helpers in \code{\link[googlesheets4]{cell-specification}}. Can combine sheet name and cell
range ("Sheet1!A5:A") or refer to a sheet by name (\code{range = "Sheet1"},
although \code{sheet = "Sheet1"} is preferred for clarity).
\item Difference: Can NOT be a named range.
\item Difference: \code{range} can be interpreted as the \emph{start} of the target
rectangle (the upper left corner) or, more literally, as the actual
target rectangle. See the "Range specification" section for details.
}}

\item{col_names}{Logical, indicates whether to send the column names of
\code{data}.}

\item{theme}{A \code{\link[=gs_theme]{gs_theme()}} object, a preset name string, or \code{NULL}
(see above).}
}
\value{
The input \code{ss}, as a \code{sheets_id}, invisibly.
}
\description{
The \code{theme}-based counterpart to \code{\link[=range_write_format]{range_write_format()}}: writes \code{data}
with \code{googlesheets4::range_write(..., reformat = FALSE)}, then applies
\code{theme} via the same engine behind the \verb{gs_theme_*()} presets -- header
style, optional banding, per-column type-inferred formatting (with any
\code{theme$columns} overrides layered on top), column widths, an optional
border, \code{theme}'s conditional formats, and freezing the header row.
}
\details{
\code{theme} can be:
\itemize{
\item a \code{\link[=gs_theme]{gs_theme()}} object, built by hand or via \code{\link[=modify_theme]{modify_theme()}}
\item the bare name of a built-in preset (\code{"clean"}, \code{"professional"},
\code{"fun"}, or \code{"stylish"}) -- shorthand for
\code{gs_theme_clean()}/etc., but returning control to \code{write_pretty_sheet()}
instead of applying immediately
\item omitted entirely, in which case \code{write_pretty_sheet()} looks
for a theme attached to \code{data} -- see \code{\link[=read_pretty_sheet]{read_pretty_sheet()}}, which
attaches the theme it detects on a sheet as \code{attr(data, "prettysheets_theme")} -- so
\code{write_pretty_sheet(ss2, data = read_pretty_sheet(ss1))} reuses the
original sheet's look automatically. Falls back to a plain default
theme (bold header, light gray background, no banding/border) if
neither is available.
}

Like \code{\link[=range_write_format]{range_write_format()}}, \code{sheet} doesn't need to already exist -- a
missing sheet name is created first (sized to fit \code{data}) instead of
erroring.
}
\examples{
\dontrun{
ss <- googlesheets4::gs4_create("prettysheets-write-demo")
write_pretty_sheet(ss, data = mtcars, sheet = "Sheet1", theme = "professional")

my_theme <- gs_theme(
  header = list(background_color = "#1F4E79", font_color = "white"),
  columns = list(mpg = list(number_format = "0.0"))
)
write_pretty_sheet(ss, data = mtcars, sheet = "Sheet2", theme = my_theme)
}
}
