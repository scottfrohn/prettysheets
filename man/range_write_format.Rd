% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write-format.R
\name{range_write_format}
\alias{range_write_format}
\title{Write a data frame and format it in one call}
\usage{
range_write_format(
  ss,
  data,
  sheet = NULL,
  range = NULL,
  col_names = TRUE,
  header_bold = TRUE,
  header_background_color = "#F3F3F3",
  header_font_color = NULL,
  freeze_header = TRUE,
  conditional_formats = list()
)
}
\arguments{
\item{ss}{Something that identifies a Google Sheet:
\itemize{
\item its file id as a string or \code{\link[googledrive:drive_id]{drive_id}}
\item a URL from which we can recover the id
\item a one-row \code{\link[googledrive:dribble]{dribble}}, which is how googledrive
represents Drive files
\item an instance of \code{googlesheets4_spreadsheet}, which is what \code{\link[googlesheets4:gs4_get]{gs4_get()}}
returns
}

Processed through \code{\link[googlesheets4:as_sheets_id]{as_sheets_id()}}.}

\item{data}{A data frame.}

\item{sheet}{Sheet to write into, in the sense of "worksheet" or "tab". You can identify a sheet by name, with a string, or by position, with a number. Ignored if the sheet is specified via \code{range}. If neither argument specifies the sheet, defaults to the first visible sheet.}

\item{range}{Where to write. This \code{range} argument has important similarities
and differences to \code{range} elsewhere (e.g. \code{\link[googlesheets4:range_read]{range_read()}}):
\itemize{
\item Similarities: Can be a cell range, using A1 notation ("A1:D3") or using
the helpers in \code{\link[googlesheets4]{cell-specification}}. Can combine sheet name and cell
range ("Sheet1!A5:A") or refer to a sheet by name (\code{range = "Sheet1"},
although \code{sheet = "Sheet1"} is preferred for clarity).
\item Difference: Can NOT be a named range.
\item Difference: \code{range} can be interpreted as the \emph{start} of the target
rectangle (the upper left corner) or, more literally, as the actual
target rectangle. See the "Range specification" section for details.
}}

\item{col_names}{Logical, indicates whether to send the column names of
\code{data}.}

\item{header_bold, header_background_color, header_font_color}{Passed to
\code{\link[=range_format]{range_format()}} for the header row, if \code{col_names = TRUE}. Set
\code{header_bold = NULL} (and leave the color args \code{NULL}) to skip header
styling entirely.}

\item{freeze_header}{Logical; freeze the header row via \code{\link[=sheet_freeze]{sheet_freeze()}}.
Only applies if \code{col_names = TRUE}.}

\item{conditional_formats}{A list of formatting calls to apply after
writing, each itself a \code{list(range = ..., rule = ..., format = ...)} for
a boolean rule, or \code{list(range = ..., gradient = ...)} for a gradient
rule. Optional.}
}
\value{
The input \code{ss}, as a \code{sheets_id}, invisibly.
}
\description{
Combines \code{\link[googlesheets4:range_write]{googlesheets4::range_write()}} with \code{prettysheets}'s formatting
functions. Internally calls \code{range_write(..., reformat = FALSE)} \emph{before}
applying formatting — \code{range_write()}'s default \code{reformat = TRUE} clears
existing formatting on write, which would otherwise immediately erase
whatever this function had just applied.
}
\details{
Unlike \code{range_write()} itself, \code{sheet} doesn't need to already exist --
if it's a name not already in the spreadsheet, an empty (work)sheet by
that name is added first (\code{range_write()} alone errors in this case,
since -- unlike \code{googlesheets4::sheet_write()} -- it doesn't create
sheets on demand), sized to fit \code{data} (plus a header row, if
\code{col_names}) rather than left at the API's own default grid size.
}
\examples{
\dontrun{
ss <- googlesheets4::gs4_create("prettysheets-write-demo")
range_write_format(
  ss, data = mtcars, sheet = "Sheet1", range = "A1",
  header_background_color = "#D9E1F2",
  conditional_formats = list(
    list(range = "F2:F33", rule = cf_cell_value(">", 3.5),
         format = cf_format(background_color = "#F4CCCC"))
  )
)
}
}
