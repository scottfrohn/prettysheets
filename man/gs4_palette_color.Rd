% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palette.R
\name{gs4_palette_color}
\alias{gs4_palette_color}
\title{Look up a Google Sheets default palette color by name}
\usage{
gs4_palette_color(name)
}
\arguments{
\item{name}{Color name as it appears in \link{gs4_palette} (e.g.
\code{"cornflower blue"}, \code{"light cornflower blue 1"}). Matching is
case-insensitive, ignores leading/trailing whitespace, and treats
\code{"grey"} and \code{"gray"} as the same word (e.g. \code{"dark grey 1"} matches
\link{gs4_palette}'s \code{"dark gray 1"}) -- \link{gs4_palette}'s own \code{name} column
only spells it \code{"gray"}, but the two are interchangeable everywhere
else in R (\code{"gray"}/\code{"grey"} are already aliases for
\code{\link[grDevices:col2rgb]{grDevices::col2rgb()}}), so this extends the same equivalence to the
numbered/modified palette names that plain \code{col2rgb()} doesn't know
about (\code{"dark gray 1"}, \code{"light gray 2"}, etc. aren't valid base R
color names on their own).}
}
\value{
The hex code, as a string, e.g. \code{"#4a86e8"}.
}
\description{
Look up a Google Sheets default palette color by name
}
\examples{
gs4_palette_color("cornflower blue")
gs4_palette_color("Dark Red 2")
gs4_palette_color("dark grey 1") # same as "dark gray 1"
}
