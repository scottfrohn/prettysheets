% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{gs_theme_presets}
\alias{gs_theme_presets}
\alias{gs_theme_clean}
\alias{gs_theme_professional}
\alias{gs_theme_fun}
\alias{gs_theme_stylish}
\title{Default "pretty" table themes, pipeable after writing a sheet}
\usage{
gs_theme_clean(ss, sheet = NULL, data = NULL, wrap_long_text = FALSE)

gs_theme_professional(ss, sheet = NULL, data = NULL, wrap_long_text = FALSE)

gs_theme_fun(ss, sheet = NULL, data = NULL, wrap_long_text = FALSE)

gs_theme_stylish(ss, sheet = NULL, data = NULL, wrap_long_text = FALSE)
}
\arguments{
\item{ss}{Something that identifies a Google Sheet, processed through
\code{\link[googlesheets4:as_sheets_id]{googlesheets4::as_sheets_id()}}.}

\item{sheet}{Sheet to theme, by name or position. Defaults to the first
visible sheet.}

\item{data}{Optional; the data frame already written to \code{sheet}. If
omitted, it's read back live via \code{\link[googlesheets4:range_read]{googlesheets4::range_read()}}.}

\item{wrap_long_text}{Logical; whether \code{"long_text"} columns (character
data over 50 characters) wrap onto multiple lines. Default \code{FALSE},
so they're clipped to one line -- like a \code{"url"} column -- rather
than growing every row to fit the tallest cell.}
}
\value{
\code{ss}, invisibly, so this chains with \verb{|>}/\verb{\%>\%}.
}
\description{
Apply a consistent look to a table you just wrote: bold header, a fill
color for the header and alternating body rows (as a native Sheets
"banded range" -- editable afterward via Format > Alternating colors in
the Sheets UI), top-aligned cells, and type-appropriate column
formatting inferred from the data (whole numbers centered with
thousands separators, decimals centered at 2 places, short text
left-aligned and sized to its content, URLs left-aligned, clipped to
one line, and each one turned into an actual clickable hyperlink to
itself (a formatting property, not an \verb{=HYPERLINK()} formula -- the
cell's text and value are untouched), and long text (over 50
characters) left-aligned and given a fixed width instead of sized to
its (unwrapped) content -- clipped to one line by default, or wrapped
if \code{wrap_long_text = TRUE}. Every column is at least 100px wide,
however short its content.
}
\details{
\code{data} is optional. Supply it (the same data frame you just wrote) to
skip an extra read of the sheet; omit it and the theme reads the
written values back live to infer column types -- which is what makes
\code{googlesheets4::sheet_write(data, ss) |> gs_theme_clean()} work as a
pipe, since \code{sheet_write()} returns \code{ss}, not \code{data}.

Re-theming a table already themed by one of these functions (or already
carrying alternating colors applied some other way, e.g. by hand in the
Sheets UI) works fine: \code{sheet_clear_format()} -- which every
\verb{gs_theme_*()} call runs first -- removes any existing banded
range/alternating colors overlapping the table before the new one is
added, since the Sheets API doesn't allow two banded ranges to overlap
the same cells.

Column widths are estimated from content (header and formatted data,
whichever is wider) rather than read back from
\code{\link[googlesheets4:range_autofit]{googlesheets4::range_autofit()}}, since there's no API call that reports
what autofit actually decided for a column -- which means, unlike that
\code{googlesheets4} function, sizing columns doesn't send its own request
outside this package's usual batching: every \verb{gs_theme_*()} call (widths
included) goes through the same queueing as the rest of \code{prettysheets}, so
wrapping one in \code{\link[=prettysheets_batch]{prettysheets_batch()}} \emph{does} reduce it to a single API call.
}
\section{Functions}{
\itemize{
\item \code{gs_theme_clean()}: Neutral grayscale: dark gray header, white/light
gray alternating rows.

\item \code{gs_theme_professional()}: Corporate blue: dark blue header, white/pale-blue
alternating rows, thin blue border around the whole table.

\item \code{gs_theme_fun()}: Bright and playful: magenta header, alternating
pastel yellow/blue rows.

\item \code{gs_theme_stylish()}: Modern and moody: near-black header, white/lavender
alternating rows, thin purple border around the whole table.

}}
\examples{
\dontrun{
ss <- googlesheets4::gs4_create("prettysheets-theme-demo", sheets = mtcars)
gs_theme_clean(ss)

# or, piped straight from writing the data, with no `data` argument:
googlesheets4::sheet_write(mtcars, ss, sheet = "mtcars") |> gs_theme_professional()
}
}
