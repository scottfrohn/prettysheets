% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sheet-structure.R
\name{sheet_format_header}
\alias{sheet_format_header}
\title{Style the header row of a range you wrote with \code{range_write()}}
\usage{
sheet_format_header(
  ss,
  sheet = NULL,
  row = 1,
  bold = TRUE,
  background_color = "#F3F3F3",
  font_color = NULL,
  freeze = TRUE
)
}
\arguments{
\item{ss}{Something that identifies a Google Sheet, processed through
\code{\link[googlesheets4:as_sheets_id]{googlesheets4::as_sheets_id()}} (a Sheet id, URL, dribble, or
\code{googlesheets4_spreadsheet}).}

\item{sheet}{Sheet to format, by name or position. Defaults to the first
visible sheet, or the sheet named in \code{range} if \code{range} is a
\code{cell_limits} object built from \code{"SheetName!A1:B2"}.}

\item{row}{Row to style as a header. Defaults to \code{1}.}

\item{bold, background_color, font_color}{Passed straight through to
\code{\link[=range_format]{range_format()}}; defaults give a bold header with a light grey
background, matching the look \code{sheet_write()} applies automatically.}

\item{freeze}{Logical; also freeze this row (and everything above it) via
\code{\link[=sheet_freeze]{sheet_freeze()}}. Default \code{TRUE}.}
}
\value{
The input \code{ss}, as a \code{sheets_id}, invisibly.
}
\description{
\code{googlesheets4::sheet_write()} already applies header styling and freezes
row 1 automatically. Use this when you wrote data with
\code{\link[googlesheets4:range_write]{googlesheets4::range_write()}} instead (which does \emph{not} auto-style), and
want the same "table" look applied manually.
}
