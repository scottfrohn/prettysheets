% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{prettysheets_batch}
\alias{prettysheets_batch}
\title{Batch multiple \code{prettysheets} formatting calls into a single API request}
\usage{
prettysheets_batch(code)
}
\arguments{
\item{code}{Code containing one or more \code{prettysheets} formatting calls.}
}
\value{
The \code{sheets_id} of the spreadsheet that was updated, invisibly.
}
\description{
Every \code{prettysheets} function normally sends its own \code{batchUpdate} call as
soon as it runs, which is simple but costs one network round trip per
call. Wrap a block of \code{prettysheets} calls in \code{prettysheets_batch()} to
collect their requests and send them all together in one call instead —
useful when you're applying several formatting operations to the same
spreadsheet in a row and want to reduce API traffic (the Sheets API's
documented per-user rate limit is 100 requests per 100 seconds).
}
\examples{
\dontrun{
prettysheets_batch({
  range_format(ss, range = "A1:D1", bold = TRUE)
  range_format(ss, range = "A1:D1", background_color = "#F4CCCC")
})
}
}
