% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sheet-structure.R
\name{sheet_clear_format}
\alias{sheet_clear_format}
\title{Clear all cell formatting for a sheet, or a range within it}
\usage{
sheet_clear_format(ss, sheet = NULL, range = NULL)
}
\arguments{
\item{ss}{Something that identifies a Google Sheet, processed through
\code{\link[googlesheets4:as_sheets_id]{googlesheets4::as_sheets_id()}} (a Sheet id, URL, dribble, or
\code{googlesheets4_spreadsheet}).}

\item{sheet}{Sheet to format, by name or position. Defaults to the first
visible sheet, or the sheet named in \code{range} if \code{range} is a
\code{cell_limits} object built from \code{"SheetName!A1:B2"}.}

\item{range}{\code{NULL} (the default) clears the whole sheet. Pass an A1
string or \code{cellranger} helper to clear only that range instead.}
}
\value{
The input \code{ss}, as a \code{sheets_id}, invisibly.
}
\description{
Resets fonts, colors, number formats, alignment, and text wrap back to
the spreadsheet default. This is the same reset \code{\link[=range_format]{range_format()}}'s
\code{clear_first = TRUE} performs before applying new formatting, exposed on
its own for when you just want a clean slate without immediately
formatting anything. This also removes any existing banded range
(alternating colors -- from the Sheets UI or a \verb{gs_theme_*()} call) and
any conditional-format rule (from \code{\link[=range_add_conditional_format]{range_add_conditional_format()}},
\code{\link[=range_add_gradient_format]{range_add_gradient_format()}}, or the Sheets UI) that overlaps \code{range},
since both are separate sheet-level objects \code{userEnteredFormat} doesn't
cover and clearing it wouldn't otherwise touch. Note this still doesn't
affect borders — a separate property again; see \code{\link[=range_format_border]{range_format_border()}}
(\code{style = "NONE"}) to clear those.
}
\examples{
\dontrun{
ss <- googlesheets4::gs4_create("prettysheets-demo", sheets = mtcars)
sheet_clear_format(ss)
sheet_clear_format(ss, range = "A1:K1")
}
}
\seealso{
Makes a \code{RepeatCellRequest}:
\url{https://developers.google.com/workspace/sheets/api/reference/rest/v4/spreadsheets/request#repeatcellrequest}
}
