% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cols.R
\name{gs4_cols}
\alias{gs4_cols}
\title{Reference a range by column name instead of column letter}
\usage{
gs4_cols(..., data = NULL, header_row = 1)
}
\arguments{
\item{...}{One or more column names (character strings).}

\item{data}{Optional; a data frame (its \code{names()} are used) or a
character vector of column names, matched against \code{...} with no API
call — handy right after \code{\link[=range_write_format]{range_write_format()}} with the same \code{data}.
If omitted, column names are instead read live from the sheet's header
row when the range is resolved.}

\item{header_row}{Row number of the header. Determines which row is
read (when \code{data} is not supplied) and which row is excluded from the
selected range. Default \code{1}.}
}
\value{
A \code{prettysheets_cols} object.
}
\description{
Pass the result as \code{range} to \code{\link[=range_format]{range_format()}}, \code{\link[=range_format_border]{range_format_border()}},
or any other \code{prettysheets} function that takes a \code{range}, to target one or
more columns by their header name (e.g. \code{"mpg"}) instead of a column
letter or A1 string. Only the data rows below the header are selected —
the header row itself is left untouched, so styling a column by name
never re-styles its header.
}
\details{
If the named columns aren't contiguous in the sheet (e.g. \code{"mpg"} and
\code{"wt"} with another column between them), this expands into one
\code{GridRange} per contiguous run, and the calling function sends one
request per run — so formatting still reaches every named column, just
as more than one API request under the hood.
}
\examples{
\dontrun{
ss <- googlesheets4::gs4_create("prettysheets-demo", sheets = mtcars)
range_format(ss, range = gs4_cols("mpg", "hp"), bold = TRUE)
range_format(ss, range = gs4_cols("mpg", data = mtcars), number_format = "0.0")
}
}
