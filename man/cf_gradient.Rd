% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional-format.R
\name{cf_gradient}
\alias{cf_gradient}
\title{Build a gradient (color-scale) rule}
\usage{
cf_gradient(
  min_color,
  mid_color = NULL,
  max_color,
  min_type = "MIN",
  mid_type = NULL,
  max_type = "MAX",
  min_value = NULL,
  mid_value = NULL,
  max_value = NULL
)
}
\arguments{
\item{min_color, mid_color, max_color}{Colors for the low, middle (optional),
and high end of the scale.}

\item{min_type, mid_type, max_type}{How each point's value is determined:
one of \code{"MIN"}, \code{"MAX"}, \code{"NUMBER"}, \code{"PERCENT"}, \code{"PERCENTILE"}.
Defaults are \code{"MIN"} for the low end and \code{"MAX"} for the high end.}

\item{min_value, mid_value, max_value}{Numeric value for the point, required
when the corresponding \verb{*_type} is \code{"NUMBER"}, \code{"PERCENT"}, or
\code{"PERCENTILE"}.}
}
\value{
A list suitable as the \code{gradient} argument of
\code{\link[=range_add_gradient_format]{range_add_gradient_format()}}.
}
\description{
Build a gradient (color-scale) rule
}
\examples{
cf_gradient(min_color = "white", max_color = "forestgreen")
}
