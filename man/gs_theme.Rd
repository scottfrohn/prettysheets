% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{gs_theme}
\alias{gs_theme}
\title{Build a reusable table style for \code{write_pretty_sheet()}/\verb{gs_theme_*()}}
\usage{
gs_theme(
  header = list(),
  banding = NULL,
  columns = list(),
  border = NULL,
  wrap_long_text = FALSE,
  freeze_header = TRUE,
  conditional_formats = list()
)
}
\arguments{
\item{header}{A list with any of \code{bold} (logical, default \code{TRUE}),
\code{background_color}, \code{font_color} (colors -- hex, named R color, or
\link{gs4_palette} name). Applied to row 1 directly via \code{\link[=range_format]{range_format()}},
regardless of whether \code{banding} is also set.}

\item{banding}{\code{NULL} (the default, no zebra-striping) or a list with
\code{band1}/\code{band2} colors, added as a native Sheets "banded range" (the
same construct as Format > Alternating colors in the Sheets UI). The
banded range's header row color reuses \code{header$background_color}
(falling back to \code{band1} if that's \code{NULL}), so the two mechanisms
never disagree.}

\item{columns}{A named list, one entry per column you want to override
the type-inferred default for (see \code{classify_column()}/
\code{theme_column_format()}), keyed by the column's header name, e.g.
\code{list(price = list(number_format = "$#,##0.00"))}. Each entry is a
list of \code{\link[=range_format]{range_format()}}-style arguments, layered on top of (not
replacing) that column's type-based default.}

\item{border}{\code{NULL} (the default, no border) or a list with \code{color}
(required), \code{sides} (default \code{"outside"}), \code{style} (default
\code{"SOLID"}) -- see \code{\link[=range_format_border]{range_format_border()}}.}

\item{wrap_long_text}{Logical; whether \code{"long_text"} columns wrap onto
multiple lines instead of clipping to one. Default \code{FALSE}.}

\item{freeze_header}{Logical; freeze row 1 via \code{\link[=sheet_freeze]{sheet_freeze()}}.
Default \code{TRUE}.}

\item{conditional_formats}{A list of formatting calls to apply after
theming, each either \code{list(range = ..., rule = ..., format = ...)}
for a boolean rule (see \code{\link[=cf_cell_value]{cf_cell_value()}} and friends, \code{\link[=cf_format]{cf_format()}})
or \code{list(range = ..., gradient = ...)} for a gradient rule (see
\code{\link[=cf_gradient]{cf_gradient()}}). \code{range} is commonly a \code{\link[=gs4_cols]{gs4_cols()}} selection, so the
rule keeps applying to the right columns regardless of how many rows
of data you write.}
}
\value{
A \code{prettysheets_theme} object.
}
\description{
A \code{gs_theme()} is a bundle of formatting decisions -- header style,
zebra-striping colors, per-column overrides, a table border, and
conditional-format rules -- captured as one inspectable, reusable value
instead of a pile of individual \code{\link[=range_format]{range_format()}}/\code{\link[=range_add_conditional_format]{range_add_conditional_format()}}
calls. Pass it to \code{\link[=write_pretty_sheet]{write_pretty_sheet()}}'s \code{theme} argument, or build on
one of the built-in presets (\code{\link[=gs_theme_clean]{gs_theme_clean()}} and friends only \emph{apply}
a theme immediately; see \code{gs_theme_presets} for how they're built)
with \code{\link[=modify_theme]{modify_theme()}}.
}
\examples{
my_theme <- gs_theme(
  header = list(bold = TRUE, background_color = "#1F4E79", font_color = "white"),
  banding = list(band1 = "white", band2 = "#DCE6F1"),
  columns = list(price = list(number_format = "$#,##0.00")),
  border = list(color = "gray40")
)
my_theme
}
