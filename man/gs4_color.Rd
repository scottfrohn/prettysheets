% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{gs4_color}
\alias{gs4_color}
\title{Convert a color specification to the Sheets API \code{Color} schema}
\usage{
gs4_color(x, alpha = NULL)
}
\arguments{
\item{x}{A color, or \code{NULL}.}

\item{alpha}{Optional alpha channel, 0-1.}
}
\value{
A list with \code{red}, \code{green}, \code{blue} (and optionally \code{alpha}), each in
\verb{[0, 1]}, as required by the Sheets API's \code{Color} object. \code{NULL} if \code{x}
is \code{NULL}.
}
\description{
Accepts a hex string (e.g. \code{"#F4CCCC"}), a named R color (e.g. \code{"steelblue"},
anything \code{\link[grDevices:col2rgb]{grDevices::col2rgb()}} understands), or an already-built list with
\code{red}/\code{green}/\code{blue} components (passed through unchanged, so you can
construct a \code{Color} object by hand if you need \code{alpha} control the helper
doesn't expose yet).
}
\examples{
gs4_color("#F4CCCC")
gs4_color("steelblue")
}
